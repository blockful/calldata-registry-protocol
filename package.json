{
  "name": "calldata-review-protocol",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter e2e test"
  },
  "engines": {
    "node": ">=18.18"
  }
}
